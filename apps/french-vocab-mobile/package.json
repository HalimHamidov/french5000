{
  "name": "french-vocab-mobile",
  "version": "1.0.0",
  "description": "French 5000 vocabulary mobile app with spaced repetition",
  "scripts": {
    "prepare": "node scripts/prepare.js",
    "dev": "node scripts/prepare.js && npx serve www -l 3000",
    "cap:add-android": "npx cap add android",
    "cap:sync": "node scripts/prepare.js && npx cap sync",
    "cap:open": "npx cap open android",
    "build:apk": "node scripts/prepare.js && npx cap sync android && cd android && ./gradlew assembleDebug",
    "build:aab": "node scripts/prepare.js && npx cap sync android && cd android && ./gradlew bundleRelease"
  },
  "dependencies": {
    "@capacitor/android": "^8.2.0",
    "@capacitor/core": "^8.2.0",
    "@capacitor/local-notifications": "^8.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.2.0",
    "serve": "^14.2.0"
  }
}
